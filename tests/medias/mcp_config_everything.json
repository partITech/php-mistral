{
  "mcp": {
    "servers": {
      "everything": {
        "command": "docker",
        "args": [
          "run",
          "-i",
          "--rm",
          "mcp/everything"
        ]
      }
    }
  }
}